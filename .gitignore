# Examples 1 and 2 demonstrate that only source/config files (.md, .html) are present
# No build artifacts, dependencies, or temporary files detected in the changes
# Therefore, no entries need to be added to .gitignore